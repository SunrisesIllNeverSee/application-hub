# Archived Donor Scaffold

This folder is archived on 2026-05-21 after the live extension was consolidated into:

- `/Users/dericmchenry/Desktop/application-hub/webextension/application-hub`

Useful pieces harvested into the live extension:

- `hubUrl` + JWT connection settings
- Canonical Hub capture flow
- Smart Matcher trigger
- Simpler page capture / Markdown export wiring

Why it was archived:

- keeping both folders live made the repo ambiguous
- `application-hub/` already had the richer side panel, BYOK generation, fill, save-to-bank, and Safari fallback lane
- the donor scaffold is now reference material, not the surviving product
