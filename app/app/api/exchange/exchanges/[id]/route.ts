import { NextRequest, NextResponse } from 'next/server'
import { authenticateCompany, authenticateProposer, getExchangeAdmin } from '@/lib/exchange/server'
export async function GET(req:NextRequest,{params}:{params:Promise<{id:string}>}){const publicId=(await params).id; const admin=getExchangeAdmin(); const {data:record,error}=await admin.from('exchange_records').select('*').eq('public_id',publicId).maybeSingle(); if(error||!record) return NextResponse.json({error:'Exchange not found'},{status:404}); const proposer=authenticateProposer(record,req.headers.get('x-exchange-proposer-key')); const company=await authenticateCompany(record.target_domain,req.headers.get('x-exchange-company-key')); if(!proposer&&!company) return NextResponse.json({error:'Unauthorized'},{status:401}); const {data:events}=await admin.from('exchange_events').select('event_type,actor,from_state,to_state,payload,created_at').eq('exchange_id',record.id).order('created_at',{ascending:true}); const {data:settlement}=await admin.from('exchange_settlements').select('*').eq('exchange_id',record.id).maybeSingle(); const safe={...record,proposer_key_hash:undefined}; return NextResponse.json({exchange:safe,events:events??[],settlement})}
