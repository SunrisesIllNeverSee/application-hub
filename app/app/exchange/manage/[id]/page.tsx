import type { Metadata } from 'next'
import Link from 'next/link'
import { ExchangeManageClient } from '@/components/exchange/ExchangeManageClient'
export const metadata:Metadata={title:'Manage Contribution Exchange',robots:{index:false,follow:false}}
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;return <main className="min-h-screen bg-neutral-950 px-5 py-12 text-neutral-100"><div className="mx-auto max-w-5xl"><div className="flex items-center justify-between"><Link href="/exchange" className="text-sm text-neutral-400">← Exchange Gateway</Link><code className="text-xs text-neutral-600">{id}</code></div><h1 className="mt-7 text-4xl font-bold">Contribution Exchange</h1><p className="mt-3 max-w-3xl text-neutral-300">One shared lifecycle for proposal, negotiation, commitment, authorization, delivery, verification and settlement. Credentials remain in your browser session only.</p><div className="mt-9"><ExchangeManageClient publicId={id}/></div></div></main>}
